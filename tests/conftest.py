# Semaine 5 : fixtures pytest à implémenter ici
#
# À prévoir :
# - client FastAPI de test (httpx.AsyncClient + app)
# - base de données SQLite en mémoire (engine in-memory, tables créées au setup)
# - mock IBMPAClient via app.dependency_overrides[get_ibm_pa_client]
# - fixture de serveurs IBM PA simulés (liste de dicts au format OData)
