@../../docs/agent-rules/git-workflow.md

# Claude Code adapter — git workflow

Canonical rule: `docs/agent-rules/git-workflow.md`.

No `paths:` frontmatter on purpose: git discipline (branche par harness,
proposition de commit après chaque action) applies globally, not to a file
scope. This adapter only ensures Claude Code always loads the neutral rule. Do
not duplicate project rules here.
